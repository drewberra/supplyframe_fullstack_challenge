<div
    id="projectsContainer"
>
    <% for (let i=0; i < projects.length; i++) { %>
        <div>
            <h2>
                <%= projects[i].name  %>
            </h2>

            <div>
                <%= projects[i].summary %>
            </div>
        </div>
    <% } %>
</div>